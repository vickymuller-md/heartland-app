import { describe, it } from 'vitest';

describe('JWT Role Injection (DBSC-05)', () => {
  it.todo('custom_access_token_hook injects user_role for provider');
  it.todo('custom_access_token_hook injects user_role for patient');
  it.todo('user_role claim matches profiles.role, not user_metadata');
});
