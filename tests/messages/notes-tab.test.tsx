/**
 * Notes Tab -- Provider Messages Section Tests
 * Requirement: MSG-05
 * Source: HEARTLAND Protocol v3.3 -- Phase 19 Structured Provider Messages
 *
 * MSG-05: Message history visible in patient detail Notes tab
 * - Renders "Previous Notes" section when notes exist
 * - Renders "Messages from Provider" section when messages exist
 * - Renders no messages section when messages array is empty
 * - Each message entry shows subject, body, and "Read X ago" or "Unread" badge
 */

// import { ProviderNotes } from '@/app/(provider)/patients/[patientId]/_components/provider-notes';

import { describe, it } from 'vitest';

describe('Notes tab -- messages section (MSG-05)', () => {
  it.todo(
    'renders "Previous Notes" section when notes exist'
  );

  it.todo(
    'renders "Messages from Provider" section when messages exist'
  );

  it.todo(
    'renders no messages section when messages array is empty'
  );

  it.todo(
    'each message entry shows subject, body, and "Read X ago" or "Unread" badge'
  );
});
